## [1.1.0] - 2026-02-20

### Added
- Release for marketplace

### Changed
- Documentation updates

# Changelog - infra

## [1.0.0] - 2026-02-19

### Added
- Skill `infra:memory` : création et optimisation de fichiers CLAUDE.md et `.claude/rules/` pour la mémoire Claude Code
- Skill `infra:skill-creator` : guide complet pour créer des skills Claude Code efficaces
- Skill `infra:make-subagent` : création de sous-agents Claude Code spécialisés
- Skill `infra:alias-add` : création d'alias de slash commands
- Skill `infra:bump` : mise à jour automatique des versions de plugins avec détection du type
- Skill `infra:init` : initialisation du marketplace et vérification des dépendances
- Skill `infra:fix-grammar` : correction grammaticale de fichiers en préservant le formatage
- Agent `infra/action` : exécuteur conditionnel d'actions (vérifie avant d'agir)
- Agent `infra/meta-agent` : générateur de nouveaux agents Claude Code
- Répertoire `hooks/` : documentation des hooks disponibles dans `customize/` et `notifications/`
