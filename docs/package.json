{
  "name": "claude-marketplace-docs",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate && vitepress dev",
    "build": "npm run generate && vitepress build",
    "preview": "vitepress preview",
    "generate": "npm run generate:docs && npm run generate:usecases",
    "generate:docs": "tsx ../scripts/generate-docs.ts",
    "generate:usecases": "tsx scripts/generate-usecases-index.ts"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2",
    "vitepress": "^1.5.0",
    "vue": "^3.5.13"
  }
}
